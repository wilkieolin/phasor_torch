# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
.python-version

# Generated parity fixtures (regenerable via julia_parity/generate_*.py).
julia_parity/fixtures/

# Julia
julia_parity/Manifest.toml
*.cov
*.mem

# HPO outputs (ytopt search trajectory + per-trial artifacts/checkpoints)
results.csv
results.json
ytopt.log
# local run outputs + the convenience symlink to them
hpo_runs/
hpo_runs
# full-data confirmation outputs + the convenience symlink to them
hpo_confirm/
hpo_confirm
# PBS job stdout files (<name>.o<jobid>, e.g. phasor_confirm.o8555898)
*.o[0-9]*
# libEnsemble run artifacts
ensemble.log
libE_stats.txt
ensemble_*/
*.npy
libE_*.pickle

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
